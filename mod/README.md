# Mod Directory

+ `assets-fallback/` this directory contains original Mindustry textures required for sprite packing
+ `assets-raw/` this directory contains raw assets, mostly sprites
+ `assets/` this directory contains post-processed sprites and assets that not required processing (maps, `.ogg`s, bundles, etc.)
+ `src/` there is all mod source code
