package nitis.gravillaso.graphics;

import arc.*;
import arc.files.*;
import arc.graphics.*;
import arc.graphics.g2d.*;
import arc.graphics.gl.*;
import arc.util.*;
import mindustry.*;
import mindustry.graphics.*;
import nitis.gravillaso.*;
import nitis.gravillaso.world.temperature.*;

import static mindustry.Vars.*;

public class ThermalRenderer{
    public ThermalRenderer(){ // TODO: implement environment renderer to draw glowing around warm blocks or something
        /*Fi root = Vars.mods.getMod(GravillasoMod.class).root;
        this.shader = new ThermalShader(
            root.child("shaders").child("thermal.vert").readString(),
            root.child("shaders").child("thermal.frag").readString()
        );*/
    }
}
